"use strict";

// LOOSE EQUALITY

// Loose equality (==)
// Loose equality (!=)

var num1 = 5;

var maybeNum2 = "5";

console.log(num1 * maybeNum2);
console.log(typeof(num1 * maybeNum2));
console.log(typeof(num1 + maybeNum2));

if(num1 == maybeNum2){
    console.log("These are equivalent");
}
if(num1 === maybeNum2){
    console.log("These are equivalent");
}

// STRICT EQUALITY (===)
// STRICT EQUALITY (!==)

// The comparators MUST be of the same data type
//  AND have the same value.
// **** for now only use strict equality (===)****

// for git ( to push it

// git status
// git add js/shorthand-unary-and-jsmethods.js
// git status
// git commit -m "adds shorthand, unary, and jsmethods materials"
// git push origin main


// GIT CLONE
// this is a test....//

