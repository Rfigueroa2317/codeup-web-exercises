/* TODO:Write a function called getSquare.
 *  It should take in one input and return the square
  * (not square root) of that input as a number.
  * If the input is not a number or numeric string,
  * getSquare should return false.*/

// function getSquare(input) {
//     if(input === Number){
//         return Math.pow(input,2);
//     }else{
//         return false;
//     }
// }
// console.log(getSquare(Math.pow(7,2)));

// function getSquare(input) {
//     if (input === null || isNaN(Number(input))) {
//         return false;
//     } else {
//         return (Math.pow(input, 2));
//     }
// }
//     console.log(getSquare(null))



/* TODO:Write a function called getReverse.
 *  It should take in one input and return the same string,
 *  in reverse. If the input is not a string (a number, boolean, etc)
 *  or is a numeric string ("45"), then return false.
 */

        function getReverse(input){
            if(input !== 'string'){
                return input.reverse;
            }else{
                return false;
            }
        }

        console.log(getReverse('Hello'))



/* TODO:Write a function called multiplySquares().
     It should take in two inputs, square each input,
      then return the product of mulitplying them together.
       If the input is not a number (5) or numeric string ("5"),
        the method should return false
 */

        // function multiplySquares(num1, num2){
        //
        // }




/*TODO: Write a function called addTo. It should take in one input
    and return the result of adding 3 to that input. If the input
     is not a number or numeric string, addTo should return false
 */

        function addTo(input){
            if(){

            }
        }



/* TODO: Write a function called subtractFrom. It should take in
     one input and return the result of subtracting 3 from that input.
      If the input is not a number or numeric string, subtractFrom
       should return false.
 */





/* TODO: Write a function called getLowestNumber. It should take
     in three inputs and determine which input is the lowest number.
      If ANY of the inputs are not numbers or numeric strings,
       getLowestNumber should return false
 */




/* TODO: Write a function called isAString. It should take in
     one input and return true if the input is a string. If the
      input is not a string, then return false
 */