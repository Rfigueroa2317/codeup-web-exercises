(function(){
    "use strict";

    //number - 5
    //string - "dog"
    //boolean - false/true
    //null - the absence of (this is a data type, not always though)
    //undefined - data should exist, but doesn't
    // object ->

    // [] <-- this is a square bracket

    // var myArr = ["dog", "cat", "fish"] // <- each comma-delimited value is called an "element"
    // console.log(Array.isArray(myArr));
    // arrays are objects

    // var myCharacteristics = ["Ricardo" , "Bass player", "30"] // 0, 1, 2
    //console.log("The length of the array is: " + myCharacteristics.length);
    // The first element in an array has an index of 0
    // the last element in an array has an index of (length - 1)
    // Arrays are 0 index
    //console.log(myCharacteristics[0])
    // console.log(`Hello, I am ${myCharacteristics[1]} named ${myCharacteristics[0]}`);



    // TODO: Make a new array which holds elements based on your own characteristics
    //  -> In a console.log, output a string which describes yourself based on those elements
    //  -> KEEP IN MIND ZERO-INDEXING!

    // var myCharacteristics = ["Ricardo" ,"Codeup student", "Bass guitar", "30"]
    // console.log(`Hello, I am ${myCharacteristics[1]} named ${myCharacteristics[0]}.
    // I play the ${myCharacteristics[2]} and I am ${myCharacteristics[3]} years old`);
    //
    // //console.log(myCharacteristics[4]); // accessing and index out of the bounds of the array.
    //
    // myCharacteristics[0] += "Figueroa"; //strings are immutable.
    // // myCharacteristics [0] = myCharacteristics[0] + "Figueroa";
    // console.log(myCharacteristics[0]);

    // for (let i = 0; i < 0; i++){

        // do something with ONE array element

    //}

    // var myPets = ["Cat", "Noodles", "Nissan"];
    //
    // // length = 3
    // // maxIndex = 2
    //
    // for (let i = 0; i <myPets.length; i++){ // if i += 2 it would skip the second one. if i += 3 it would skip the third one...
    //     console.log(myPets[i]); // 3
    // }

    // TODO: MINI-EXERCISE
    //  -> Create an array with at least 5 elements, describing whatever you'd like!
    //  -> Create a loop which will log each element to the console
    //  -> Wrap that loop in a function which has an array parameter
    //  -> Call the function and pass in your array as the argument
    //  -> DID YOU KNOW: You can add different data types to an array??
    //      -> Try adding strings, numbers, or booleans as elements
    // ** MAKE SURE YOU DO THIS MINI EXERCISE ** //

    // myPets.forEach(function(pet ){ //callback – Function to execute on each element. It accepts between one and three arguments:
    //     // do stuff
    //     console.log("My pet's name is " + pet)
    // })




// sort method //

    // function sortMyNums(num1, num2, num3){
    //     var numArray = [num1, num2, num3];
    //     //console.log(numArray);
    //     numArray.sort(function(a, b, ){
    //         console.log(a);
    //         console.log(b);
    //         console.log(a - b);
    //         return a - b; // if it were b - a it will show reversed in the console
    //     });
    //     console.log(numArray);
    //     return numArray;
    // }
    // //console.log(numArray);
    // sortMyNums(13, 1, 1009)



    function giveMissyJustice(notASuspectString){

    }


})()